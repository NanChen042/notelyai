# 谷记账开发日记：从记事本到批次记账 App

## 一、方向调整

项目最初叫 Notely AI，原计划是智能记事本。但最新需求更明确：做一个类似“谷记账”的移动端批次记账 App，用来记录每个批次的进货支出、邮费、手续费和卖出收入，并自动算出利润。

这次调整后，项目的重点从“笔记内容管理”转为“批次账本 + 本地数据 + 移动端交互”。

## 二、核心需求

- 收支类别要覆盖：进货支出、邮费、手续费、包装费、卖出收入、其他收入。
- 新增记录时只填类别、金额、备注、日期和所属批次，不做单价、数量。
- 每次卖出都新增一条记录，不覆盖历史卖出数据。
- 总收入、总支出、净利润和批次利润要自动更新。
- 数据存储在本地浏览器，刷新后仍保留。
- 代码要按正规前端方式组织，使用组件化和 Pinia。
- 界面参考 App 设计图，使用项目自带的 Vant、TailwindCSS 和 ApexCharts。
- AI 助手要像工具执行层一样工作：用户正常聊天，AI 拆出待添加记录，用户确认后再真正写入账本。

## 三、实现方案

### Pinia 账本状态

新增 `src/stores/bookkeeping.ts` 作为核心 store，维护两个主要数据：

- `batches`：批次账单。
- `records`：收支记录。

收入和支出不靠用户手动选择，而是由类别自动映射：

```ts
const categoryTypeMap = {
  进货支出: 'expense',
  邮费: 'expense',
  手续费: 'expense',
  包装费: 'expense',
  卖出收入: 'income',
  其他收入: 'income',
}
```

这样新增“邮费”或“手续费”时会自动计入支出，新增“卖出收入”时会自动计入收入，利润计算始终保持一致。

### 组件化拆分

账本相关 UI 拆到了 `src/components/bookkeeping/`：

- `SummaryCard.vue`：首页利润卡片。
- `BatchCard.vue`：批次卡片。
- `RecordTimeline.vue`：历史记录时间线。
- `RecordForm.vue`：新增收支记录弹窗。
- `BatchForm.vue`：新建批次弹窗。

`HomeView.vue` 负责页面组合和 tab 切换，不再把所有逻辑堆在一个文件里。

### 本地持久化

Pinia store 使用 LocalStorage 保存数据：

```ts
localStorage.setItem(STORAGE_KEY, JSON.stringify({
  batches: batches.value,
  records: records.value,
}))
```

读取时会恢复批次和历史记录，因此“批次账单每次卖出都要新填一次，上次历史数据也包含在内”这个需求可以自然满足。

## 四、界面调整

页面改成移动端 App 结构：

- 首页：今日收入/支出/净额、本月收入/支出、趋势图、批次账单。
- 批次：横向批次切换、批次详情、历史记录。
- 统计：利润趋势、收支占比。
- 我的：本地账本概览。

新增记录使用底部弹窗，类别使用分段按钮，整体贴近设计图里的移动端账本体验。

## 五、警告处理

本次还同步处理了两个构建警告来源：

- `postcss-px-to-viewport` 会打印 PostCSS 8 迁移警告，因此当前 `postcss.config.cjs` 不再启用该旧插件。
- ApexCharts 体积较大，会触发 Vite 默认 chunk 提示，因此在 `vite.config.ts` 中设置了 `build.chunkSizeWarningLimit: 1000`。

## 六、第二轮体验打磨

后续迭代重点转向移动端体验和统计能力。

### 主题统一

原先组件里散落了不少 Tailwind 颜色类和硬编码色值，例如深色文字、绿色按钮、收入支出颜色等。这样短期能跑，但后续换主题或调整 Vant 组件主色时容易出现不一致。

现在主题集中放到 `src/assets/main.css`：

- `--app-primary`：账本绿主色。
- `--app-income`：收入和正利润。
- `--app-expense`：支出、负利润和删除操作。
- `--app-page`、`--app-surface`、`--app-text`：页面背景、卡片和文字层级。
- `--van-primary-color`、`--van-text-color` 等 Vant 变量映射到应用主题。

同时，金额颜色判断也抽到了 `src/utils/format.ts`，组件不再各自写 `profit >= 0 ? ... : ...`。视图层只表达语义：这是收入、支出或利润，具体颜色由工具函数统一返回。

### 底部操作和 Tabbar

原先“新增记录”是一个居中的悬浮按钮，会遮住列表内容。现在改为 `header / body / footer` 的 App 布局，底部 `Tabbar` 作为 footer 常驻；中间内容区域使用 `flex-1` 独立滚动，避免导航层级和遮挡问题。

中间的“记一笔”按钮放进 `Tabbar`，用更大的加号强调主操作，点击后直接进入新增记录页。

### 批次侧滑操作

批次卡片接入 `van-swipe-cell`：

- 右侧滑出“编辑”和“删除”。
- 编辑复用 `BatchFormPage`，可以修改批次名称和封面图。
- 删除前使用 Vant 确认弹窗。
- 删除批次时，同步删除该批次下的所有收支记录，避免出现无法归属的历史记录。

Store 对应新增：

```ts
updateBatch(batchId, payload)
deleteBatch(batchId)
```

### 统计维度扩展

统计页从“全部时间”扩展为日、周、月、年四个维度：

- 日：展示今日。
- 周：展示本周每日。
- 月：按 4 天聚合展示，避免整月日期挤在图表上。
- 年：按月份展示全年趋势。

图表在只有一天数据时会显示数值标签；没有数据时给默认空状态，避免页面出现一块空白。

### AI 账单助手

AI 助手从首页内嵌页面改成了独立路由 `/ai-assistant`。它不再生成一套动态表单和表格，而是回到真正的逻辑层：

- 用户输入：“我今天买了个栗子十块钱，运费五块钱”。
- 也可以点击输入框旁的麦克风，通过语音识别生成同样的文本。
- 助手回复：“好的，接下来请确认您的需求”。
- 页面弹出确认清单，用户先选择已有批次，或新建一个批次账单，再查看类别、备注、金额和日期。
- 用户点击确认后，组件调用 Pinia store 自动新增记录。

模型接入预留为 SiliconFlow 的 `Qwen/Qwen2.5-7B-Instruct`。AI 页右上角可以本地填写 API Key，输入框用密码模式且不在页面展示明文；也支持 `VITE_SILICONFLOW_API_KEY`。没有配置时先用本地解析兜底，保证核心流程可以继续使用。

## 七、当前结果

项目现在已经完成从智能记事本到批次记账 App 的核心转型，功能上可以新增批次、新增收支、保留历史记录、实时更新利润，并通过 AI 助手把自然语言转成可确认的记账动作。后续可以继续补充记录筛选、批次完成状态切换、数据导出和更细的统计维度。
